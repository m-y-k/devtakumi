# Database
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/devtakumi?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=devtakumi
SPRING_DATASOURCE_PASSWORD=devtakumipassword

# JWT
JWT_SECRET=change-me-to-a-long-random-secret-at-least-256-bits

# Admin credentials (seeded on first startup)
ADMIN_EMAIL=admin@devtakumi.local
ADMIN_PASSWORD=admin12345
ADMIN_NAME=Admin

# CORS origins
PUBLIC_SITE_URL=http://localhost:5173
STUDENT_PORTAL_URL=http://localhost:5174

# SMTP (Brevo free tier)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_FROM=help@devtakumi.com

# Storage
STORAGE_ROOT_PATH=./storage
RECORDINGS_ROOT_PATH=./storage/recordings

# Judge0
JUDGE0_URL=http://localhost:2358

# UPI ID (configurable via DB setting)
UPI_ID=myk22.wallet@phonepe
